---
agent: "domain-implementer"
description: "Implement the human-approved Sell Stocks plan and run verification"
---

Confirm that `plan.md` is explicitly approved and still matches both specifications. Use the
`implement-sell-stocks` skill to execute the plan in order. Keep changes inside the domain-only
scope, update plan checkboxes honestly, run focused tests, then run the full verification.

Stop and ask if the plan is not approved, a specification conflict appears, or a required change
would exceed scope.
